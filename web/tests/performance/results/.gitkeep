# Performance Baseline Results

This directory stores performance baseline reports in JSON format.

Baselines are generated by running:
```bash
npm run test:performance:baseline
```

Files are named: `baseline-YYYY-MM-DD.json`

These files are tracked in git to enable historical performance analysis.
