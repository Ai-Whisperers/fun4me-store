name: Claude Code

on:
  pull_request:
    types: [opened, synchronize, reopened]
  issues:
    types: [opened, reopened, labeled]
  issue_comment:
    types: [created]

jobs:
  claude:
    runs-on: ubuntu-latest
    # Don't block PR merges if claude fails (workflow changes require merge to main first)
    continue-on-error: true
    permissions:
      contents: write
      pull-requests: write
      issues: write
      id-token: write
    steps:
      - uses: actions/checkout@v6
      
      - name: Run Claude Code
        uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
